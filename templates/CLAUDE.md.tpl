# <项目名> — Claude 操作手册

> **如果用户明确让你"继续项目"或贴了 ONBOARDING 口令，优先读 `ONBOARDING.md`。**
>
> 新 Claude 进门先读这份，30 秒掌握地形。详细进度见 `STATUS.md`，完整设计见 `<设计文档路径>`。

## 一句话

<项目名>：<一句话说项目是做什么、技术栈、服务谁>

## 🔴 地理：源码在哪，部署目标在哪

| 位置 | 角色 | 路径 |
|------|------|------|
| **本地开发机** | 源码主分支（唯一） | `<本地绝对路径>` |
| **GitHub** | 远程备份 | `<GitHub URL>`（private/public） |
| **部署目标** | 生产/测试环境 | `<部署目标描述>`（SSH 别名/URL/无） |

**禁忌：**
- ❌ 不要在部署目标上 `git init` 或 `git commit`
- ❌ 不要把部署目标源码认作主分支
- ❌ <项目专属禁忌>

## 🔧 部署工作流

<根据项目实际情况填写。以下是 macOS 交叉编译 Go 项目到远程 Mac/Linux 的示例>

```bash
# 1. 本地编译
cd <项目路径>
<编译命令>

# 2. 传 binary 到部署目标
<传输命令>

# 3. 部署目标上替换 + 重启
<重启命令>
```

或直接用一键脚本：
```bash
bash scripts/deploy.sh
```

## 🔌 远程访问（如有）

- **SSH 直连**：`ssh <别名>` 走 <直连方式，如 Cloudflare Tunnel / 局域网 / 公网>
- **不要用**：<已失效或不可靠的连接方式>

## 🏗️ 关键服务

| 服务 | 端口/地址 | 容器/路径 |
|------|-----------|-----|
| <服务 1> | | |
| <服务 2> | | |

## 🧱 技术栈

- <编程语言 版本>
- <主要框架>
- <数据库 / 向量库>
- <依赖的外部服务>

## 🗂️ 代码地图

```
<项目根>/
├── cmd/             # 服务入口
├── internal/
│   ├── <pkg1>/      # <作用>
│   └── <pkg2>/      # <作用>
├── scripts/
│   └── deploy.sh    # 部署脚本
└── configs/
    ├── config.yaml           # 真实配置（gitignore）
    └── config.example.yaml   # 脱敏示例（入库）
```

## 🧪 检查服务健康

```bash
# 查进程
<检查进程命令>
# 查日志
<查日志命令>
# 查依赖服务
<检查依赖命令>
```

## 🔑 敏感文件（已 gitignore，永不入库）

- `configs/config.yaml` — 含真实密钥
- `bin/` — 编译产物
- `data/` — 运行时数据
- `<其他敏感>`

全局密钥见 `~/.claude/projects/*/memory/credentials.md`。

## 📚 权威文档位置

| 文档 | 路径 | 用途 |
|------|------|------|
| 新会话开场手册 | 本仓库 `ONBOARDING.md` | 新 Claude 进门读 |
| 会话结束下班手册 | 本仓库 `OFFBOARDING.md` | 收尾 checklist |
| 当前进度 | 本仓库 `STATUS.md` | 进度快照 |
| 运维救命手册 | 本仓库 `RUNBOOK.md` | 9 章场景 + git 速查 |
| 部署脚本 | 本仓库 `scripts/deploy.sh` | 一键部署 |
| 设计文档（如有） | `<Obsidian/其他位置>` | 完整设计 |
| 密钥备份 | `~/.claude/projects/*/memory/credentials.md` | 所有密钥 |
