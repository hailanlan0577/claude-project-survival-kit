# 🆘 新 Claude 会话开场 — 60 秒进入状态

> **你是新 Claude，用户刚让你读这份文件。读完后用中文汇报你理解的项目状态，然后问用户下一步要做什么。**
>
> 用户是非程序员，请用生活化中文解释技术细节，不要甩英文术语给他。

## 💡 触发方式（给用户看）

1. **最快** —— 打 `/<PROJ>-onboard` 斜杠命令
2. **随口说** —— "继续 <项目名>" / "接手项目"，Claude 会识别关键词自动触发
3. **完整口令** —— 复制下面这段：
   ```
   继续之前的 <项目名> 项目。
   请先读 <项目绝对路径>/ONBOARDING.md
   读完用中文汇报当前状态，然后问我下一步。
   ```

---

## 📍 地形（30 秒必读）

| 维度 | 答案 |
|------|------|
| **源码唯一主分支** | `<源码绝对路径>` |
| **远程备份** | `<GitHub URL>`（private/public） |
| **部署目标** | `<部署方式：比如 ssh xxx / 云服务器 / 本地 / 无>` |
| **用户操作系统** | `<例：macOS Darwin 25, MacBook Pro M1 Max 64GB>` |

### 🚫 N 大禁忌（重蹈覆辙过的坑，绝不能再犯）

<!-- 项目起步时可以先留 3 条通用禁忌，以后踩到新坑再加进来 -->

1. **不要在部署目标上 `git init` 或编辑源码** — 部署目标不是开发地
2. **<项目专属禁忌 2>**
3. **<项目专属禁忌 3>**

---

## 🎯 项目是什么（10 秒）

**<项目名>** — <一句话说项目是做什么的，解决什么问题，服务谁>

技术栈：<编程语言 + 主要框架 + 关键依赖>

---

## 📊 当前状态（截至 YYYY-MM-DD）

| Phase | 状态 | 说明 |
|-------|------|------|
| 0 基础设施 | ✅/⚠️/⏳ | <说明> |
| 1 <Phase 1 名字> | ✅/⚠️/⏳ | <说明> |
| 2 <Phase 2 名字> | ✅/⚠️/⏳ | <说明> |
| 3 <Phase 3 名字> | ✅/⚠️/⏳ | <说明> |

### 🔴 当前阻塞 / 主攻目标

<用中文说清楚目前卡在哪、为什么卡、怎么解>

---

## 🚦 下一步候选（用户还没拍板）

| 选项 | 动作 | 代价 | 推荐 |
|------|------|------|------|
| **A** | <方案 A> | <代价> | |
| **B** | <方案 B> | <代价> | |
| **C** | <方案 C> | <代价> | ⭐ 推荐 |

**你要做的**：问用户选哪个，不要自己拍板。

---

## 📚 更多信息去哪找

读这些文档的**优先顺序**（别一上来就读全量设计文档，会炸上下文）：

| 文档 | 什么时候读 |
|------|-----------|
| `STATUS.md`（本仓库） | 想知道 Phase 细节、每天做过什么 |
| `CLAUDE.md`（本仓库） | 想部署、想连服务器、想了解代码地图 |
| `RUNBOOK.md`（本仓库） | 服务挂了 / 要备份 / 要重建 / 紧急救命 |
| `scripts/deploy.sh` | 要部署改动 |
| `<外部设计文档路径>` | 最后读，可能很长，分块用 offset/limit |

---

## 🛠️ 常用操作速查

```bash
# 进部署目标（如果是远程服务器）
ssh <服务器别名>

# 看服务日志
<查日志命令>

# 一键部署
cd <项目路径> && bash scripts/deploy.sh

# 只重启
bash scripts/deploy.sh --restart

# 健康检查
# 详见 RUNBOOK.md § 7
```

---

## ⚠️ 上一次会话 Claude 踩的坑

新 Claude 请注意这些心智模型错误，不要再犯：

<!-- 每次 offboard 时追加，最多保留近 5 次的关键教训 -->

1. **<2026-MM-DD>**: <坑的简述 + 教训>
2. ...

---

## 🎬 你现在该做什么

按顺序：

1. **用中文**总结你对项目状态的理解（3-5 句话）
2. 问用户：要继续干嘛？建议的下一步：
   - 决定当前阻塞的修复路径（A/B/C）
   - 或者其他工作选项
3. **等用户回复再动手**，不要自己决定就开干

---

## 🛑 会话结束前的职责（重要）

如果你感觉到上下文接近满了（开始频繁 summarize / 文件引用模糊 / 连续 30+ 轮没有结束），**主动提醒用户**：

> "感觉上下文接近满了，要不要我按 OFFBOARDING.md 做一次收尾，让下一个窗口无缝接上？"

用户确认后，读 `OFFBOARDING.md` 执行 8 步收尾 checklist。

祝玩得愉快！
